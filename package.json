{
  "name": "researchbuddy",
  "private": true,
  "version": "0.1.0",
  "description": "Guided self-education and research digest tool for medicine and the life sciences",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @researchbuddy/core",
    "test": "npm run test --workspace @researchbuddy/core",
    "typecheck": "npm run typecheck --workspace @researchbuddy/core && npm run typecheck --workspace @researchbuddy/mobile",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify": "npm run typecheck && npm run lint && npm test"
  },
  "license": "UNLICENSED",
  "overrides": {
    "react-native": "0.86.2"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "eslint-config-expo": "^57.0.1",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6"
  }
}
